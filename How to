# WARP Installer for AMD64 & ARM64

This script automates the installation of Cloudflare WARP client using WireGuard.

## Quick Installation

To install, run the following command in your terminal:

```bash
curl -O https://raw.githubusercontent.com/Iman-GTR/warp-installer/main/install_warp.sh && chmod +x install_warp.sh && sudo ./install_warp.sh
